<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuan.Dao.LiDao">
    <select id="SelectLi" resultType="com.yuan.Tool.Data">
        select * from data
    </select>

    <insert id="insertData">
        insert into data(tupian,wenjian,jieshao,lianjie,mingzi) values(#{tupian},#{wenjian},#{jieshao},#{lianjie},#{mingzi})
    </insert>
</mapper>